# Module `spatial`

> Target: 256-Core 4D-Torus Neuromorphic Photonic Processor

## ⚡ Functions & Intrinsics

### `fn broadcast_4d_sphere(payload: linear vec4_i8) -> () `(inline zero-overhead)``

### `fn open_simplex_pipe(target_axis: u32, channel_id: u32) -> (u32, u32) `(inline zero-overhead)``

### `fn sync_neighborhood(axis_mask: u32) -> () `(inline zero-overhead)``

