[package]
name = "cron-lsp"
version = "0.1.0"
edition = "2021"
description = "Language Server Protocol (LSP 3.17) server for CRON Language"

[dependencies]
cronc = { path = "../cronc" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
